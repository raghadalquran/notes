'use strict';
//Exports a constructor function
function Aconsfunc(){
this.id = ;
this.noteText = ;
}
//Has a prototype method called execute() that executes the correct operation, given the above object
Aconsfunc.prototype.execute = function(){

}
//How will you use that object to run the correct method?
//You can predict that add won’t be the only action we’re going to have to handle…

//Write a prototype method called add() that will create an object representing a note (with an ID and the note text
//as properties) and console.log the text of the note to be added when the add command is executed

Aconsfunc.prototype.add = function (){

}
